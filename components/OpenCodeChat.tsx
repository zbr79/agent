// Removed: OpenCodeChat was a second, non-persisted chat window that ran on
// the same local opencode agent as the main ChatApp (/) page. It is fully
// redundant — ChatApp already drives the agent and adds history, titles,
// share, and resume. The /opencode page now redirects home and /api/opencode
// is a 410 stub.
export {};
